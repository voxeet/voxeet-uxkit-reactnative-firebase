
import { NativeModules } from 'react-native';

const { RNVoxeetFirebase } = NativeModules;

//this module is doing nothing specifically on the React Native side. It's there to init the Firebase implementation :thumbsup:
export default RNVoxeetFirebase;
